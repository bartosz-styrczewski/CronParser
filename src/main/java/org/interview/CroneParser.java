package org.interview;

/**
 * TODO document me
 */
public class CroneParser {
    public ParsingResult parseExpression(String expression) {

        Tokenizer tokenizer = new Tokenizer();
        String[] tokens = tokenizer.tokenize(expression);

        return null;
    }
}
